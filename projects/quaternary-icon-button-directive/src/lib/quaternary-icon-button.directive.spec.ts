import { QuaternaryIconButtonDirective } from './quaternary-icon-button.directive';

describe('QuaternaryIconButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new QuaternaryIconButtonDirective();
    expect(directive).toBeTruthy();
  });
});
