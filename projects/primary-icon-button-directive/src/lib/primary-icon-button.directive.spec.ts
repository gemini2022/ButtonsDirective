import { PrimaryIconButtonDirective } from './primary-icon-button.directive';

describe('PrimaryIconButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new PrimaryIconButtonDirective();
    expect(directive).toBeTruthy();
  });
});
