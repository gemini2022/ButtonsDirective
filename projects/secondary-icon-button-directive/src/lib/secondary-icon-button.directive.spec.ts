import { SecondaryIconButtonDirective } from './secondary-icon-button.directive';

describe('SecondaryIconButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new SecondaryIconButtonDirective();
    expect(directive).toBeTruthy();
  });
});
