import { TertiaryButtonDirective } from './tertiary-button.directive';

describe('TertiaryButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new TertiaryButtonDirective();
    expect(directive).toBeTruthy();
  });
});
