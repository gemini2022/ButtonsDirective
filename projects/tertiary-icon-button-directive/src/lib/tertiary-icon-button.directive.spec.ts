import { TertiaryIconButtonDirective } from './tertiary-icon-button.directive';

describe('TertiaryIconButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new TertiaryIconButtonDirective();
    expect(directive).toBeTruthy();
  });
});
