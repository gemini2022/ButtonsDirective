import { QuaternaryButtonDirective } from './quaternary-button.directive';

describe('QuaternaryButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new QuaternaryButtonDirective();
    expect(directive).toBeTruthy();
  });
});
